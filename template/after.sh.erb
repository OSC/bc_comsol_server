# Wait for the COMSOL Server to start
echo "Waiting for COMSOL Server to open port ${port}..."
if wait_until_port_used "${host}:${port}" 60; then
  echo "Discovered COMSOL Server listening on port ${port}!"
else
  echo "Timed out waiting for COMSOL Server to open port ${port}!" ; exit 1
fi
sleep 2
